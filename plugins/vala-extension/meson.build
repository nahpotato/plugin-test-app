library(
  'vala-extension',
  'vala-extension.vala',
  dependencies: [
    pta_dep,
    dependency('libpeas-1.0', version: '>=1.0')
  ],
  install: true,
  install_dir: pkglibdir / 'plugins',
)

install_data(
  'vala-extension.plugin',
  install_dir: pkglibdir / 'plugins',
)
